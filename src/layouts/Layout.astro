---
const baseUrl = import.meta.env.BASE_URL;
const { title = 'Saved Press - Your Personal Weekly Digest' } = Astro.props;
---

<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="description" content="Transform how you consume content with Saved Press. Save articles from anywhere, get a beautifully curated weekly newspaper." />
    <meta name="theme-color" content="#ffffff" />
    <title>{title}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
  </head>
  <body class="font-sans antialiased">
    <slot />
    <footer class="bg-paper py-12">
      <div class="max-w-7xl mx-auto px-4 text-center">
        <div class="flex justify-center space-x-6">
          <a href={`${baseUrl}privacy-policy`} class="text-text-secondary hover:text-primary">Privacy Policy</a>
          <a href={`${baseUrl}terms`} class="text-text-secondary hover:text-primary">Terms & Conditions</a>
        </div>
      </div>
    </footer>
  </body>
</html>

<style is:global>
  :root {
    --font-sans: 'Inter', system-ui, -apple-system, sans-serif;
  }

  html {
    font-family: var(--font-sans);
  }
</style>